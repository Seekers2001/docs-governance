# 测试清单与测试开发协作方案

> 状态：2026-07-12 §20 最终复审 PASS；v1 已实现（skill + TESTS 模板 + 判定点职责迁移），下一步只剩真实项目试点。§16 的 PASS 和历史 CHANGES_NEEDED 保留作评审证据。
> 项目路径：`/Users/daoer/Desktop/0-GitHub/docs-governance`

## 0. 拍板记录（2026-07-12，阿磊）

| # | 待拍板项 | 结论 |
|---|---|---|
| 1 | 判定点清单归属 | **搬家派**：TESTS.md 是"规则→测试→缺口"的唯一台账；REGRESSION.md 只留下游+验收命令+联动规则，判定点以 TEST-ID 回引；同步修订 module-regression skill 与模板（见 §3.1） |
| 2 | v1 范围 | **砍到"模板 + skill"**，不建 agent、不建 command（见 §11）；agent/command 形态试点后再定 |
| 3 | 试点项目 | 经营报表加工系统（默认，如换项目阿磊另行指定） |
| 4 | `最近验证`字段 | **删除**：执行历史不存档，验证靠审计时现场重跑命令取退出码；PROJECT_LOG 只追加两类一行事件——测试点状态翻转、交付节点回归判决 |
| 5 | 模板状态集 | 修正为 待补/开发中/已覆盖/不适用 四态，概况区与状态集一致（见 §5） |
| 6 | 判定点迁移范围 | **一次搬干净**：同步处理判定覆盖率、坑必下沉铁律和 governance-retro 指路牌；迁移完成后做全仓机械检查（见 §3.1） |
| 7 | v1 测试执法方式 | **不开发检查脚本**：由主会话按 skill 人工核验文件、节点和退出码；确定性脚本放到二期（见 §11） |
| 8 | 测试维护入口 | **两个入口汇入同一 TESTS.md**：主会话扫描并分类现有测试，判断必要/重复/缺失/疑似废弃；同时读取 Bug 清单，每个有效 Bug 必须登记 TEST-ID 并补回归保护（见 §4.1-§4.4） |
| 9 | Hermes 借鉴范围 | **借纪律，不搬重型基础设施**：每个项目登记标准测试入口和默认/专用分组；E2E 写清真实/模拟边界；Bug 保留真实故障形状；断言优先行为契约与不变量（见 §4.5） |

## 1. 背景

`docs-governance` 当前主要治理两类事实：

1. 活文档是否仍然反映真实代码和项目状态；
2. 模块发生改动后，应该运行哪些已有测试，防止“改 A 坏 B”。

第二类能力已经由模块回归体系承担：

- 方法论：`skills/module-regression/SKILL.md`
- 审查 Agent：`agents/regression-auditor.md`
- 命令入口：`commands/regression-audit.md`
- 台账模板：`templates/REGRESSION.example.md`

但目前还缺少一层更靠前的测试协作治理：

> 一个项目究竟有哪些重要规则需要测试？哪些规则已经覆盖？哪些仍是测试缺口？新增需求或修复 Bug 时，应该补什么测试？

现有 `REGRESSION.md` 能回答“改完以后跑什么”，不能完整回答“本来应该测什么”和“为什么要测”。因此拟新增一份测试清单与测试协作方法论；专职测试审查 Agent 属二期选项，v1 不实现。

## 2. 目标

新增测试协作能力后，希望形成下面的闭环：

```text
入口 A：扫描项目已有测试          入口 B：需求 / 业务规则 / Bug 清单
        ↓                                  ↓
形成测试资产地图                   登记必要测试点与 TEST-ID
        └──────────────┬───────────────────┘
                       ↓
        在 TESTS.md 汇总已有能力和测试缺口
        ↓
开发者编写功能代码和对应测试
        ↓
测试审查（v1 由主会话按 skill 执行）判断测试是否真正证明了规则
        ↓
测试通过后标记为“已覆盖”
        ↓
REGRESSION.md 登记模块改动后应重跑的验收命令
        ↓
regression-auditor 在交付前执行本模块及下游回归
```

这套能力不是为了追求“测试数量更多”，而是让重要规则、测试代码和交付证据能够相互对应。

## 3. 为什么单独新增 TESTS.md

建议让两个台账保持不同职责：

| 文档 | 回答的问题 | 使用时机 |
|---|---|---|
| `TESTS.md` | 应该测试什么？为什么测试？目前有没有覆盖？ | 需求设计、开发、修 Bug、测试审查时 |
| `REGRESSION.md` | 修改某个模块以后，必须重跑哪些测试？ | 代码改动完成、准备交付时 |

不建议把二者合并成一个大表：

- 测试清单关注“规则与缺口”，属于开发前和开发中的测试规划；
- 回归台账关注“影响面与执行命令”，属于开发后的交付验证；
- 合并后容易让清单同时承担规划、覆盖、依赖和执行记录，最终难以维护。

二者通过稳定测试编号关联，例如 `TEST-ORDER-001`，避免重复抄写同一事实。

### 3.1 判定点清单迁移（搬家派拍板的落地步骤）

现有「判定点清单」住在 module-regression 体系里（`skills/module-regression/SKILL.md` 与 `templates/REGRESSION.example.md`），登记的正是"规则→执法命令→缺口"，与 TESTS.md 核心事实重叠。按 §0 拍板迁移如下，且所有功能引用必须与 TESTS.md 落地在同一次改动内完成，防止两个 skill 各教一套：

1. `skills/module-regression/SKILL.md`：删除「判定点清单」一节，原位置改为指路牌——"规则→测试→缺口登记在 TESTS.md（见 test-collaboration skill），REGRESSION.md 以 TEST-ID 回引"；
2. `templates/REGRESSION.example.md`：删除判定点清单示例，模块段只留下游/验收命令/联动规则，判定点写 TEST-ID 引用；
3. `skills/module-regression/SKILL.md`：删除由 `/regression-audit` 统计判定覆盖率的职责；判定覆盖率随判定点迁入 test-collaboration，v1 由主会话根据 TESTS.md 人工统计；
4. `skills/module-regression/SKILL.md` 铁律 4：将“登记进该模块的判定点清单”改为“登记进 TESTS.md 并取得对应 TEST-ID”；
5. `commands/governance-retro.md`：将“指向 module-regression 的判定点清单登记格式”改为指向 test-collaboration 的 TESTS.md 登记格式；
6. 迁移完成后，"某条规则有没有测试执法"这一事实只活在 TESTS.md 一处；对功能文件执行全仓机械检查，确认 module-regression、regression-audit、governance-retro 和旧模板不再拥有或指向旧判定点清单。历史 LOG 与本文评审记录不作迁移对象。

## 4. TESTS.md 管什么

`TESTS.md` 只登记重要的业务规则、关键边界、安全风险、历史 Bug 和跨模块流程，不罗列项目中的每个测试函数。

测试文件名、测试函数数量等可以从代码扫描得到，不应该全部靠人工抄进文档。人工和 Agent 主要维护代码本身看不出来的内容：

- 为什么需要这个测试；
- 它保护哪条业务规则或系统行为；
- 什么输入能够触发风险；
- 正确结果应该是什么；
- 当前是缺口、开发中，还是已有可执行证据；
- 它来自需求、事故、Bug，还是安全约束。

建议状态只有四种：

- `待补`：已经确认需要测试，但还没有测试代码；
- `开发中`：测试正在编写或修正；
- `已覆盖`：有对应测试文件、有效业务断言和可执行命令，实际运行通过，并已进入标准测试入口或具名专用任务；
- `不适用`：经过明确判断不需要自动化测试，并写明理由。

### 4.1 入口 A：盘点和维护现有测试资产

主会话按 test-collaboration skill 扫描项目现有测试目录、测试配置、包管理脚本和 CI 命令，先回答“项目现在有什么测试”。扫描对象至少包括：

- `tests/`、`test/`、`__tests__/`、`e2e/` 等测试目录；
- `pytest` markers、Vitest/Jest/Playwright 配置及 package scripts；
- CI 中真实执行的测试命令；
- 测试文件、测试节点、fixtures、golden sample 和对账脚本；
- 能安全执行时的测试收集结果，例如 `pytest --collect-only`。

扫描结果不逐函数抄进文档，而是按“模块/测试套件/运行命令”聚合成测试资产地图。每次盘点同时给出四种判断：

- `必要`：保护关键规则、关键链路或高风险边界，应保留；
- `重复`：多条测试可能在重复证明同一行为，待人工确认是否合并；
- `缺失`：关键模块或流程没有对应层级的测试；
- `疑似废弃`：测试指向已下线功能、失效路径或 CI 不再执行的命令，待人工确认，Agent 不得自行删除。

测试资产地图采用事件触发式维护，不设置容易失真的固定天数：

1. 首次采用 TESTS.md 时做一次全量盘点；
2. 测试目录、测试配置、标准运行入口或 CI 任务发生变化时，复核受影响模块；
3. 新增、关闭或改变 TEST-ID，以及新增/关闭 Bug 时，顺带复核对应模块行；
4. 重大功能、接口契约或模块依赖变化时，复核相关测试层级和缺口；
5. 发布、阶段交付或 `/governance-sync` 前，确认本阶段改动涉及的资产地图仍然真实；
6. 只有测试体系重构或地图普遍失真时才重新全量扫描，日常以受影响模块增量维护为主。

### 4.2 测试类型采用“层级 + 用途”双标签

单元、集成和端到端描述测试覆盖的**层级**；回归描述测试存在的**用途**，两者不能混成互斥分类。一条测试可以同时是“端到端测试 + 回归保护”，也可以有多个用途。用途必须从受控词表中选择，不能临时发明新值。

| 维度 | 类型 | 主要回答的问题 |
|---|---|---|
| 层级 | 单元测试 | 单个函数、规则或计算是否正确 |
| 层级 | 集成测试 | 模块、数据库、API 或外部适配器能否正确协作 |
| 层级 | 端到端测试（E2E） | 用户关键业务流程能否从入口走到最终结果 |
| 层级 | 契约测试 | 前后端或服务间字段与行为是否符合唯一契约 |
| 层级 | 冒烟测试 | 系统最基本的启动和核心入口是否可用 |
| 用途 | 规则保护 | 业务公式、字段口径、状态转换等规则是否持续正确 |
| 用途 | 关键链路 | 登录、下单、付款、报表生成等关键用户流程是否可用 |
| 用途 | 回归保护 | 历史正确行为或已修 Bug 是否不会再次出错 |
| 用途 | 专项保护 | 权限、数据泄露、性能、兼容性等专项风险是否受控 |

受控用途值仅为：`规则保护`、`关键链路`、`回归保护`、`专项保护`。需要多个用途时用逗号并列，例如 `关键链路, 回归保护`。

### 4.3 怎么判断某类测试有没有必要

测试必要性不靠“文件数量”判断，而按风险和失败后果判断：

1. 金额、权限、数据加工、外部契约、关键用户流程和历史 Bug，必须有测试；
2. 纯函数和单条计算规则优先单元测试；
3. 模块交接、数据库、文件或网络边界优先集成/契约测试；
4. 登录、下单、付款、报表生成等关键完整流程至少有一条 E2E 或真实路径验收；
5. 同一规则已有更强测试覆盖时，新增重复测试必须说明额外价值；
6. 功能已下线或测试从未进入执行链路时，标记“疑似废弃”并交给人确认，不自动删除。

### 4.4 入口 B：Bug 清单强制转测试点

主会话读取项目 Bug 清单、issue、审计问题清单或事故记录。每个已确认的有效 Bug 都必须回答：“哪一个 TEST-ID 永久防止它复发？”

```text
BUG-035
  ↓ 提取真实故障输入和预期结果
TEST-BUG-035（状态：待补）
  ↓ 修复前先确认测试失败
修复代码
  ↓ 修复后确认测试通过
TEST-BUG-035（状态：已覆盖，用途：回归保护）
```

Bug 关闭的最低证据包括：Bug 来源、复现输入、预期结果、测试层级、测试文件/节点、运行命令，以及修复前失败和修复后通过的事实。无法自动化时也必须在 TESTS.md 登记，并写清人工验收方式和不能自动化的原因。

### 4.5 从 Hermes 借鉴的测试纪律

Hermes 的测试规模和运行器不适合直接复制到所有项目，但以下纪律应进入 test-collaboration：

1. **标准测试入口**：每个项目登记一个默认测试命令，保证本地与 CI 使用相同入口；单条测试通过但未进入默认入口或专用 CI 任务，不得视为稳定覆盖。
2. **默认与专用分组**：快速、无外部依赖的测试默认运行；需要 API Key、Docker、真实平台或较长时间的集成/E2E/性能测试进入具名专用任务，并写明触发条件。
3. **E2E 边界透明**：E2E 必须写清从哪个入口到哪个出口、哪些内部路径真实执行、哪些外部依赖使用 fake/mock。不能把只测单个函数的测试命名为 E2E，也不能因为外部平台被 mock 就否认内部完整链路的 E2E 价值。
4. **Bug 保留真实故障形状**：Bug 回归测试记录原平台/环境、真实故障输入和修复前错误表现。高风险 Bug 优先同时补快速单元保护和真实路径/集成/E2E 保护。
5. **行为契约和不变量优先**：测试“数据之间必须保持的关系”和用户可观察行为，不冻结工具数量、枚举长度、当前模型列表等预期会变化的快照。
6. **fixture/fake 复用**：多个测试共享的平台事件、会话、数据库、文件样本或 API 响应，应沉淀到 `fixtures/`、`fakes/` 或测试框架公共 fixture；fake 只替代外部边缘，不应把需要验证的核心内部路径一起 mock 掉。
7. **正反断言**：高风险测试既检查正确结果存在，也检查禁止结果和副作用没有发生，例如密钥未泄露、未审批前没有提交、库存不足时没有创建订单。

不在 v1 照搬 Hermes 的内容：每文件独立进程、并行分片、耗时缓存、进程树清理和复杂 CI 矩阵。这些属于大型仓库运行基础设施，不是测试治理方法论的起步条件。

## 5. TESTS.md 建议模板

```markdown
# TESTS.md｜项目测试清单

> 只登记重要规则和测试缺口，不手工复制全部测试函数。
> 没有测试文件、运行命令和成功执行证据，不得标记为“已覆盖”。
> 不登记执行历史：验证靠审计时现场重跑运行命令；状态翻转一行追加进 PROJECT_LOG。

## 测试概况

- 待补：2
- 开发中：1
- 已覆盖：12
- 不适用：0

## 标准测试入口

- 默认测试：`<项目标准测试命令>`（本地与 CI 一致，快速、无外部依赖）
- E2E 专用：`<项目 E2E 测试命令>`
- 外部集成：`<项目外部集成测试命令>`（需要外部服务时显式运行）

## 现有测试资产地图

> 由主会话扫描项目后按模块/测试套件聚合更新，不逐函数手抄。

| 模块/流程 | 层级 | 用途 | 执行分组 | 外部依赖 | 测试位置 | 当前判断 |
|---|---|---|---|---|---|---|
| 订单金额 | 单元 | 规则保护 | 默认 | 无 | `tests/test_order.py` | 必要 |
| 订单支付 | 集成 | 回归保护 | 默认 | 本地 fake | `tests/integration/test_payment.py` | 必要 |
| 完整下单 | E2E | 关键链路 | E2E 专用 | 假支付网关 | 待补 | 缺失 |
| 旧版皮肤 | 单元 | 规则保护 | 未进入 CI | 无 | `tests/test_old_skin.py` | 疑似废弃 |

## 订单模块

### TEST-ORDER-001｜正确计算优惠后的实付金额

- 状态：已覆盖
- 测试目的：防止优惠金额修改后导致订单实付金额计算错误
- 规则来源：订单结算需求 / CONTRACT.md#订单金额
- 模拟输入：商品金额 100 元，优惠金额 20 元
- 预期结果：实付金额为 80 元
- 测试层级：单元测试
- 测试用途：规则保护
- 执行分组：默认
- 测试边界：金额计算函数输入 → 实付金额输出
- 真实部分：金额计算逻辑
- 模拟部分：无
- 测试文件：`tests/test_order.py`
- 测试节点：`test_discounted_order_amount`
- 运行命令：`pytest tests/test_order.py::test_discounted_order_amount`

### TEST-BUG-035｜库存不足时禁止创建订单

- 状态：待补
- 测试目的：防止库存为 0 时仍然创建有效订单
- 规则来源：历史 Bug #35
- 模拟输入：库存 0，购买数量 1
- 预期结果：拒绝创建订单并返回“库存不足”
- 建议层级：集成测试
- 测试用途：回归保护
- 执行分组：默认
- 测试边界：订单服务 → 库存检查 → 创建结果
- 真实部分：订单与库存模块协作
- 模拟部分：外部消息通知
- 测试文件：待补
- 运行命令：待补
```

## 6. 专职测试审查 Agent

> 2026-07-12 拍板：本节 agent 与 §8 命令均为二期设计，v1 不建（见 §11）；v1 的测试审查由主会话按 skill 人肉执行。执行边界铁律：test-auditor 跑命令只为验证"已覆盖"标记属实（审查时机）；regression-auditor 跑命令是交付门禁（交付时机）——同一动作、不同目的、不同时机，互不代劳。

二期建议新增：

- 方法论：`skills/test-collaboration/SKILL.md`
- Agent：`agents/test-auditor.md`
- 命令：`commands/test-audit.md`
- 模板：`templates/TESTS.example.md`

Agent 的定位不是“替开发者随便多写几个测试”，而是测试证据审查员：

> 审查重要规则是否有对应测试，测试是否真的覆盖了所声称的场景，以及“已覆盖”是否有可执行证据。

### 6.1 test-auditor 应该审查什么

1. **需求对应性**：新增需求、契约变更和 Bug 修复是否在 `TESTS.md` 中有对应测试点。
2. **场景真实性**：测试输入是否真的能触发风险，而不是只测最顺利的正常情况。
3. **预期明确性**：预期结果是否可判断，避免“能运行”“没有报错”这种弱结论。
4. **证据有效性**：标记“已覆盖”的条目是否存在测试文件、测试节点和运行命令。
5. **执行真实性**：登记的命令能否真实执行，退出码是否为 0。
6. **断言强度**：测试是否验证业务结果，而不是只验证函数被调用或 mock 被触发。
7. **边界覆盖**：空值、异常值、权限、安全边界和历史故障输入是否遗漏。
8. **清单漂移**：测试被删除、重命名或失效后，清单是否仍错误地标记为“已覆盖”。

### 6.2 test-auditor 不应该做什么

- 不负责扫描模块下游并决定改动后跑哪些回归测试，这是 `regression-auditor` 的职责；
- 不以测试数量或覆盖率百分比代替业务规则判断；
- 不因为测试文件存在就判定“已覆盖”；
- 审计模式下只报告问题，不直接修改生产代码或放松断言；
- 不把所有测试函数抄进 `TESTS.md`，避免制造第二份容易腐烂的文件索引。

## 7. 两个 Agent 的分工

> 本节是二期角色设计，v1 不创建 `test-auditor`，由主会话按 test-collaboration skill 执行测试盘点和测试审查。

| Agent | 核心问题 | 输入 | 输出 |
|---|---|---|---|
| `test-auditor` | 测试设计和证据够不够？ | `TESTS.md`、需求/契约、测试代码、Bug 记录 | 缺口、弱测试、失效引用、执行结果 |
| `regression-auditor` | 本次改动有没有弄坏自己或下游？ | git diff、`REGRESSION.md`、验收命令 | 本模块及下游的红绿判决 |

简单理解：

- `test-auditor` 判断“题目出得对不对、答案能不能证明规则”；
- `regression-auditor` 判断“这次改完以后，相关试卷有没有全部重新考过”。

## 8. 命令设计建议

> 2026-07-12 拍板：命令属二期，v1 不实现（见 §11）。

二期建议只提供一个命令，避免入口过多：

```text
/test-audit init       首次扫描项目，生成 TESTS.md 草稿
/test-audit            审查当前需求或代码改动涉及的测试清单
/test-audit TEST-ID    审查指定测试点
```

### init 模式

- 扫描现有测试目录、测试配置、CI 和运行脚本，按模块/测试套件形成“测试资产地图”；
- 给现有测试标注层级（单元/集成/E2E/契约/冒烟）和用途（回归/安全/性能等）；
- 判断必要、重复、缺失和疑似废弃，所有删除建议必须等人确认；
- 从项目文档、契约、历史 Bug 中寻找重要规则候选；
- 读取 Bug 清单，为每个已确认且尚无保护的 Bug 建立 TEST-ID；
- 能确定对应关系的条目标记为“待确认”；
- 找不到测试证据的规则标记为“待补”；
- 不允许仅凭测试名称自动宣称业务规则已经覆盖。

### 日常审查模式

- 根据 `git diff`、当前需求或用户指定模块确定审查范围；
- 查找对应 `TESTS.md` 条目；
- 检查测试代码与断言；
- 运行登记命令并记录退出码；
- 输出“已覆盖 / 待补 / 弱覆盖 / 引用失效 / 执行失败”。

## 9. 与开发流程的结合

### 测试维护盘点

1. 主会话扫描测试目录、配置、CI 和可执行的测试收集结果；
2. 按模块/套件汇总现有测试，标注“层级 + 用途”；
3. 对照关键模块、关键业务流程和 Bug 清单，判断必要、重复、缺失和疑似废弃；
4. 将聚合结果更新到 TESTS.md 的测试资产地图，将缺口登记为 TEST-ID；
5. 疑似重复或废弃的测试只形成建议，必须由人确认后才能修改或删除。

### 新功能

1. 需求确认后，在 `TESTS.md` 写明关键验收场景；
2. 状态先标记为“待补”；
3. 开发功能并补测试；
4. 测试审查（v1 由主会话按 skill 执行）检查测试设计和执行证据；
5. 审查通过后更新为“已覆盖”；
6. 将需要交付时重复运行的命令关联进 `REGRESSION.md`。

### Bug 修复

1. 从 Bug 清单读取已确认问题，为其建立唯一 TEST-ID；
2. 用真实故障输入新增一个能稳定复现 Bug 的测试点，并标注“测试层级 + 回归保护用途”；
3. 修复前确认测试失败，证明它确实捕获了问题；
4. 修复代码后确认测试通过；
5. 在 `TESTS.md` 记录 Bug 来源、复现输入和测试证据；
6. 由 `regression-auditor` 执行相关模块及下游回归；没有 TEST-ID 或明确的人工验收理由，Bug 不得宣称闭环。

### 交付前

1. 测试审查（v1 由主会话按 skill 执行）确认重要规则没有“口头覆盖”；
2. `regression-auditor` 运行本次改动涉及的全部回归命令；
3. 两边都没有阻塞项，才可以说明测试闭环完成。

## 10. 为什么这样做

### 10.1 测试代码只能告诉我们“测了什么”，不总能告诉我们“为什么测”

大型项目里经常存在几百甚至几千个测试，但重要业务规则仍可能遗漏。测试清单把规则、风险和测试证据连接起来，让人和 Agent 能判断覆盖是否有意义。

### 10.2 “测试通过”不等于“需求被证明”

测试可能只覆盖正常输入、mock 掉关键依赖，或者断言过弱。测试审查需要检查输入、预期和断言之间是否真的形成证据链；v1 由主会话按 skill 执行，二期再决定是否固化为专职 Agent。

### 10.3 测试缺口需要像开发任务一样被看见

没有清单时，“以后补测试”通常只存在聊天记录或人的记忆里。`TESTS.md` 把它变成可分配、可更新、可审查的项目资产。

### 10.4 规划测试和执行回归是两个不同动作

一个负责决定“应该有什么考试题”，另一个负责决定“这次改动需要重考哪些题”。职责分开后，两个 Agent 的判断边界更清楚，也更容易自动化。

### 10.5 符合 docs-governance 的非重叠原则

每个事实只保留一个主要归属：

- 测试目的、业务场景、覆盖状态与判定点只放在 `TESTS.md`（判定点清单自 module-regression 迁入，唯一归属，见 §3.1）；
- 模块下游关系和改后必跑命令只放在 `REGRESSION.md`，判定点以 TEST-ID 回引；
- 具体断言和执行逻辑只放在测试代码；
- 执行历史不存档：验证靠审计时现场重跑命令取退出码；`PROJECT_LOG.md` 只追加两类一行事件——测试点状态翻转、交付节点回归判决。

文档之间用测试 ID 和路径引用，不复制整段内容。

## 11. 第一版范围建议

第一版保持轻量（按 §0 拍板砍半），只实现：

1. `TESTS.md` 模板（`templates/TESTS.example.md`），同时包含“现有测试资产地图”和“必要测试点清单”，使用四态状态集，不含执行历史字段；
2. `test-collaboration` 方法论（`skills/test-collaboration/SKILL.md`），包含“测试盘点”和“Bug 转测试”两个工作模式、Hermes 借鉴纪律，以及双 auditor 执行边界铁律；
3. §3.1 的完整判定点迁移（方法论、模板、判定覆盖率、坑必下沉铁律和 governance-retro 指路牌同步修订）。

第一版暂不实现：

- `test-auditor` agent 与 `/test-audit` 命令（试点后再定形态：单 agent 挂双 skill vs 两个 agent，见 §14.3）；
- 自动生成所有测试函数目录；
- 强制追求某个覆盖率百分比；
- 自动修改测试或生产代码；
- 对测试文件、测试节点和运行命令做确定性检查的执法脚本；
- 复杂的外部需求管理系统同步；
- 与 `REGRESSION.md` 双向自动同步。

v1 验证方式：在试点项目（经营报表加工系统）人肉试点一轮。主会话先识别标准测试入口，扫描现有测试，形成按层级/用途和默认/专用分组分类的测试资产地图，并指出至少一个缺失、重复或疑似废弃项；再读取历史 Bug 清单，为至少一个尚无保护的 Bug 建立 TEST-ID。随后现场核验“已覆盖”条目的测试文件、测试节点、行为断言、运行命令及其是否进入标准入口/专用任务，并用真实退出码判定；E2E 还要核对真实与模拟边界。同时用回溯法验收（历史 Bug 反查，见 §14.2 第 6 条）。这些检查在 v1 是试点步骤，不是新增脚本；试点证明有价值后再下沉为二期确定性脚本。

先验证团队是否愿意持续维护测试点和缺口，再决定是否增加自动扫描、lint 或 CI 门禁。

## 12. 需要 Claude 重点评审的问题

请 Claude 阅读本项目以下文件后评审本方案：

- `/Users/daoer/Desktop/0-GitHub/docs-governance/README.md`
- `/Users/daoer/Desktop/0-GitHub/docs-governance/CLAUDE.md`
- `/Users/daoer/Desktop/0-GitHub/docs-governance/skills/module-regression/SKILL.md`
- `/Users/daoer/Desktop/0-GitHub/docs-governance/agents/regression-auditor.md`
- `/Users/daoer/Desktop/0-GitHub/docs-governance/commands/regression-audit.md`
- `/Users/daoer/Desktop/0-GitHub/docs-governance/templates/REGRESSION.example.md`
- `/Users/daoer/Desktop/0-GitHub/docs-governance/TEST_COLLABORATION_PROPOSAL.md`

重点讨论：

1. `TESTS.md` 与 `REGRESSION.md` 的职责是否足够清楚；
2. 是否存在重复维护、清单腐烂或过度治理风险；
3. `test-auditor` 的审查边界是否可执行，是否与 `regression-auditor` 重叠；
4. 哪些字段必须人工维护，哪些信息应该从代码自动扫描；
5. 第一版范围是否仍然过大，能否进一步缩小；
6. 如何证明这个功能真正减少测试遗漏，而不是只增加一份文档。
7. 测试资产地图按模块/套件聚合，是否足以维护现有单元、集成、E2E 等测试而不退化成全部测试函数索引；
8. “Bug 必须转 TEST-ID”是否有无法自动化时的合理人工验收出口。
9. “进入标准入口/具名专用任务”作为已覆盖条件，是否会误伤合理的手工验收场景；
10. E2E 的真实/模拟边界字段是否足以防止测试层级虚标。

## 13. 当前建议结论

建议继续推进，但先作为独立能力试用，不立即把它做成重量级测试管理系统。

最小成功标准：

- 一个真实项目能生成现有测试资产地图，并识别至少一个缺失、重复或疑似废弃项；
- 一个真实项目能用 `TESTS.md` 找出至少一个此前未被测试的重要规则；
- 至少一个历史 Bug 被转换成 TEST-ID，并留下复现输入、预期结果和回归保护证据；
- 项目明确一个标准测试入口，并能区分默认套件和具名专用套件；
- 至少一条 E2E 或真实路径测试写清真实部分、模拟部分和入口到出口边界；
- 补测试后，测试审查（v1 由主会话按 skill 执行）能用文件、断言和退出码证明该规则已覆盖；
- 后续修改相关模块时，`regression-auditor` 能通过 `REGRESSION.md` 找到并重跑这条测试；
- 团队维护成本低于它减少的遗漏和返工成本。

## 配套工具：双 Agent 自动讨论

本讨论稿配套提供：

- 调度脚本：`scripts/dual-agent-review-loop.py`
- 离线测试：`tests/test_dual_agent_review_loop.py`

它只是方案讨论调度脚本，不是 §11 所说的测试执法脚本。它不会让两个 Agent 同时修改正文，而是由脚本依次调用 Claude 和 Codex，将双方的只读评审结果写到本文末尾的受管区域。脚本计算正文哈希时会排除该区域，因此 Agent 写回的讨论不会再次触发自己；只有正文变化才启动下一轮。

### 先手动执行一次

在项目根目录运行：

```bash
python3 scripts/dual-agent-review-loop.py TEST_COLLABORATION_PROPOSAL.md --force
```

默认最多讨论 2 轮。Claude 和 Codex 第一行都返回 `VERDICT: PASS` 时提前停止；否则达到最大轮数后停止并等待人工判断。

### 持续监测文件变化

每 10 分钟检查一次：

```bash
python3 scripts/dual-agent-review-loop.py TEST_COLLABORATION_PROPOSAL.md --watch-interval 600
```

按 `Ctrl-C` 停止。脚本状态保存在项目 `.git/` 目录，不污染工作区，也不会被提交。

### 安全边界

- Claude 使用 `plan` 权限模式，只允许 `Read,Grep,Glob`；
- Codex 使用 `read-only` sandbox；
- 每次 Agent 调用默认 300 秒超时；
- Claude 每次调用默认预算上限为 0.50 美元，可用 `--claude-budget-usd` 调整；
- 脚本本身负责写回评审区，Agent 不获得编辑权限；
- 任何 Agent 调用失败都会返回非零退出码，不会伪造讨论完成；
- 自动讨论只给方案结论，不能自动进入正式功能实现。

### Claude 执行指令

将本文交给 Claude 后，可以直接告诉它：

> 请先阅读本文件第 12 节和第 14 节，再在项目根目录执行 `python3 scripts/dual-agent-review-loop.py TEST_COLLABORATION_PROPOSAL.md --force`。执行后检查文档末尾的双 Agent 评审区，并把仍未解决的问题报告给阿磊。不要直接实现测试治理功能。

---

## 14. Claude 评审意见（2026-07-11，已读 §12 全部七个文件）

> 2026-07-12：本节结论已由阿磊拍板（§0）并回灌正文（§0/§3.1/§5/§6/§8/§10.5/§11），保留作评审历史，不再单独维护。

> 本节由 Claude 追加，供后续评审者（扣子等）接着讨论。总结论：**方向成立，但方案有一处硬伤未处理——与 REGRESSION.md 中已有的「判定点清单」职责撞车；另外第一版范围还能砍一半。**

### 14.1 最大问题：方案通篇没提「判定点清单」

`skills/module-regression/SKILL.md` 中已存在「判定点清单」一节，干的就是 TESTS.md 想干的事：

> 把该模块"什么算对"的规则逐条列出，标注由哪条可执行命令执法……说不出"落在哪条命令上"的判定点 = 侥幸，进缺口清单。

对照：

| | 判定点清单（已有，住在 REGRESSION.md） | TESTS.md（拟新增） |
|---|---|---|
| 规则→测试的映射 | 有 | 有 |
| 缺口标记 | ⚠️ 侥幸 | 待补 |
| 覆盖证据 | 测试节点引用 | 测试文件+命令+退出码 |
| 多出来的 | — | 测试目的/规则来源/模拟输入/预期结果/状态机 |

TESTS.md 本质是判定点清单的**加强版**，不是新物种。方案 §10.5 声称符合非重叠原则，但恰恰漏审了这条最直接的重叠——同一事实（"规则 X 有没有测试执法"）会同时活在两份文档里，必然一份先烂，这正是本插件要治的病。

**两条出路，必须先拍板其一：**

1. **升级派**：不建 TESTS.md，扩充判定点清单格式（加规则来源、模拟输入、预期结果），仍住 REGRESSION.md。改动最小，但 REGRESSION.md 变胖，且"开发前规划"和"改后回归"两个时机混在一份文档。
2. **搬家派**：建 TESTS.md，但**同时把判定点清单从 module-regression 迁出**，REGRESSION.md 只留"下游+验收命令+联动规则"三要素，判定点通过 TEST-ID 引用 TESTS.md。方案该走这条路，但目前完全没写迁移步骤和对 module-regression skill 的修订——不补上，两个 skill 各教一套，插件自己内部先漂移。

Claude 倾向搬家派，前提是补上 skill 修订说明。

### 14.2 对 §12 六个问题的逐条回答

1. **职责清楚吗**：TESTS.md 与 REGRESSION.md 二者之间清楚（测什么 vs 改后重跑什么），但与判定点清单的三方关系没理清，见 14.1。
2. **腐烂风险**：两处。①`最近验证：日期+退出码`是执行历史，按非重叠原则该进 LOG 或由脚本回写，放 TESTS.md 里意味着每次审计都改这个文件，人工维护必漂移——建议删掉或标"仅脚本更新"。②`测试文件/测试节点`会因重命名失效（插件自己说过"手写清单必然腐烂"），6.1 第 8 条的漂移检查应由脚本做（文件/节点存在性），不该靠 agent 人肉看。另有一个模板自相矛盾的小 bug：「测试概况」列了`失效：0`，但四种状态是待补/开发中/已覆盖/不适用——`失效`不在状态集里，`不适用`没进概况。
3. **test-auditor 边界**：6.1 第 5 条"执行真实性"与 regression-auditor 核心动作重叠。切法：test-auditor 跑命令**只为验证"已覆盖"标记属实**（审查时机），regression-auditor 跑命令是**交付门禁**（交付时机）——同一动作、不同目的、不同时机，方案要写死这句话，否则将来两边都跑全量测试白烧。其余 7 条审查项（场景真实性、断言强度、边界覆盖等）边界干净，是方案真正的增量价值。
4. **人工 vs 自动**：人工/Agent 判断——测试目的、规则来源、模拟输入、预期结果、"不适用"的理由；脚本执法——测试文件存在性、测试节点存在性、命令退出码、失效引用（这四项建议进 verify 类脚本，不占 agent 上下文）。
5. **第一版还能砍吗**：能，砍一半。**v1 只做模板 + skill 方法论，不建 agent、不建 command。**理由：§13 自己定的最小成功标准是"真实项目找出至少一条漏测规则"——验证这件事不需要 agent，主会话照 SKILL 草稿就能当 test-auditor 用。拿经营报表项目（现成的 NaN 订单号事故、6 条业务异常清单）人肉试点一轮，真找到缺口、且愿意持续维护清单，再花成本固化 agent + command。先验证"人愿不愿意养这份文档"，再造工具。
6. **怎么证明真减少遗漏**：回溯法。init 时拿历史 Bug 反查——"如果当时有 TESTS.md，NaN 订单号那条会不会在待补清单里"。上线后指标只看一个：**后来爆的 bug 里有多少条事先躺在待补清单里**。躺过=看见了没排上（管理问题）；没躺过=清单失效（方法问题）。比"覆盖了多少条"诚实。

### 14.3 追加讨论：一个 Agent 挂两个 skill 行不行（小磊提议）

小磊提议：不分 test-auditor / regression-auditor 两个 agent，插件里写**一个测试 Agent**，按需调用 test-collaboration 和 module-regression 两个 skill。

Claude 的判断：**技术可行，但它解决的是次要问题（入口数量），解决不了 14.1 的硬伤**——撞车发生在文档层（两份台账记同一事实），不在 agent 层。就算只有一个 Agent 读写，它面对的还是两份重叠文档，改测试要同步两处，漏一处就漂移。腐烂的是文档，不是 Agent。

单 Agent 方案本身的利弊：

**利**
- 入口和文件数少，符合插件"保持轻量"的自我约束；
- skill 是方法论唯一源，一个 Agent 按需读两份不违反现有架构（docs-governor 已是此模式）。

**弊（两条实的）**
1. **成本档位锁死**：regression-auditor 现为 sonnet——故意的，纯机械活（跑命令抄退出码）、每次改动都跑、要便宜；测试设计审查（断言强度、场景真实性）是判断活，要更强模型。一个 Agent 只能填一个 model，合并后要么机械活用贵模型，要么判断活用便宜模型。
2. **门禁不能有想法**：回归审计是交付门禁，价值在于**无聊、确定、只看退出码**。合并后同一 Agent 带着判断力跑门禁，容易在该报红绿时多嘴"顺便建议加个边界测试"，交付节奏被拖住，"裁判只报不修"的铁律边界也变模糊。

**结论：此问题是末端决策，v1 按 14.2 第 5 条根本不建 agent；试点跑完自然知道测试审查多久干一次、需要多强判断力，届时再拍一个还是两个，是五分钟的决定。现在争这个顺序反了。**

### 14.4 待拍板清单（给小磊 / 扣子）

1. 判定点清单归属：升级派 or 搬家派？（Claude 推荐搬家派 + 同步修 module-regression skill）
2. v1 是否接受砍到"模板 + skill、无 agent 无 command"？
3. 试点项目是否定为经营报表加工系统？
4. `最近验证`字段：删除 or 改为仅脚本更新？
5. 模板概况区状态集修正（失效 vs 不适用）。

## 15. Codex 当前复核意见与给 Claude 的交接（2026-07-12）

### 15.1 Codex 当前结论

`CODEX VERDICT: READY_FOR_CLAUDE_REVIEW`

Codex 已只读核对当前正文与现有 `module-regression` 方法论。上一轮双方提出的两个阻塞项已经按阿磊拍板回写正文：

1. **判定点迁移范围已补齐**：§3.1 现在明确包含判定点清单正文、REGRESSION 模板、判定覆盖率、module-regression 铁律 4、governance-retro 指路牌以及迁移后的机械检查；
2. **v1 范围已自洽**：§11 已将确定性测试执法脚本移到二期，v1 仅交付模板、skill 和完整迁移；试点阶段由主会话人工核验文件、测试节点、运行命令与真实退出码。

同时已统一容易误导实现者的措辞：标题改为“测试开发协作方案”；背景、目标、新功能流程、交付流程均明确 v1 没有 `test-auditor`，测试审查由主会话按 skill 执行；配套的 `dual-agent-review-loop.py` 被明确标记为讨论调度工具，不是测试执法脚本。

Codex 当前未发现新的方向性冲突。旧的“双 Agent 自动评审”区域针对正文版本 `622aa1e51955`，属于历史评审证据，其中的 `CHANGES_NEEDED` 不代表当前正文状态。

### 15.2 请 Claude 做最后一轮只读复核

请 Claude 读取：

- 本文 §0、§3.1、§6、§9、§10.5、§11、§13 和本节；
- `skills/module-regression/SKILL.md`；
- `templates/REGRESSION.example.md`；
- `commands/governance-retro.md`；
- `commands/regression-audit.md`。

重点只确认以下问题：

1. §3.1 是否已经覆盖迁移时所有会失效的功能引用；
2. 判定覆盖率从 module-regression 搬到 test-collaboration 后，职责是否仍有重复；
3. §11 是否已经清楚表达“v1 无 agent、无 command、无测试执法脚本”；
4. v1 的人工试点步骤是否足够验证价值，又没有偷偷扩大实现范围；
5. 当前正文是否还存在会让实现者误以为 v1 已有 `test-auditor` 的措辞。

请不要运行 `dual-agent-review-loop.py`，不要修改或实现插件功能。请把评审结果直接追加在本节后面，格式如下：

```markdown
## 16. Claude 最终复核（日期）

CLAUDE VERDICT: PASS
```

如果仍有阻塞，将 `PASS` 改为 `CHANGES_NEEDED`，并只列出有文件路径和正文位置证据的阻塞项。不要重复已经解决的旧问题。

## 16. Claude 最终复核（2026-07-12）

CLAUDE VERDICT: PASS

小磊，我已只读核对 §15.2 列出的五个问题，逐条现场核实（含全仓 grep 判定点/判定覆盖率/test-auditor），未运行任何脚本、未修改除本节外的任何文件。结论：可以进入实现。

**逐项核对**

1. **§3.1 迁移清单完整性**：全仓 grep「判定点」命中 5 处（不含本讨论稿自身），其中 4 处是功能引用、1 处是历史 LOG（`PROJECT_LOG.md:15`）。§3.1 六条逐一覆盖：
   - `skills/module-regression/SKILL.md:29-42`「判定点清单」整节（含 42 行判定覆盖率描述）→ 条目 1、3；
   - `skills/module-regression/SKILL.md:64` 铁律 4 措辞 → 条目 4；
   - `templates/REGRESSION.example.md:21` 判定点清单示例 → 条目 2；
   - `commands/governance-retro.md:19` 指路牌 → 条目 5；
   - `PROJECT_LOG.md:15` → 条目 6 已明确排除在迁移对象外（历史流水账）。
   无遗漏。`agents/regression-auditor.md`、`README.md`、`CLAUDE.md`、`CLAUDE_MAP.md` 等文件 grep 均零命中，不需要额外迁移动作。

2. **判定覆盖率职责是否仍重复**：§3.1 条目 3 已把"判定覆盖率"的统计职责从 `/regression-audit` 移除，改由 test-collaboration 侧（v1 人工统计）承担，不构成两处并存。唯一 observation：§5 的 TESTS.md 模板"测试概况"只给待补/开发中/已覆盖/不适用四个计数，没有显式命名"判定覆盖率"这个指标——四个计数本身足以人工推算比率，不是阻塞项，只是指标没被正式命名，不影响实现。

3. **§11 是否已表达"v1 无 agent、无 command、无测试执法脚本"**：是。"暂不实现"清单明确列了 `test-auditor` agent 与 `/test-audit` 命令、以及"对测试文件、测试节点和运行命令做确定性检查的执法脚本"，与 §0 拍板 #2、#7 一致，不再有张力。

4. **v1 人肉试点步骤是否足够又不越界**："v1 验证方式"段落明确写"这些检查在 v1 是试点步骤，不是新增脚本"，验证路径（试点项目 + 现场核验 + 回溯法）具体可执行，没有隐含要求先写工具再试点。范围没有偷偷扩大。

5. **是否有误导性措辞**：全仓 grep `test-auditor` 只在本讨论稿命中，其余功能文件零残留。稿内 §6/§8/§9/§10.2/§13 所有 test-auditor 提法均已改写或标注"二期"/"v1 由主会话按 skill 执行"。唯一提醒（非阻塞）：§7"两个 Agent 的分工"表格本身没有独立标"二期"，只靠紧邻上方 §6 的标注定性——单独跳转到 §7 的读者可能一时看不出这是二期设计描述。建议但不要求：给 §7 也补一句"二期设计，v1 不涉及"的标注。

**结论**：五项核对均无阻塞，此前两轮指出的问题已用具体文件行号验证确已修复。PASS。

## 17. 新增测试维护机制后的 Codex 说明（2026-07-12）

阿磊补充了方案此前缺少的两个入口，并确认借鉴 Hermes 的测试纪律，现已回写正文：

1. **现有测试盘点**：主会话扫描测试目录、配置、CI 和可执行的测试收集结果，按模块/测试套件聚合为资产地图，标注层级、用途以及必要/重复/缺失/疑似废弃；
2. **Bug 转测试**：读取 Bug/issue/审计问题清单，每个有效 Bug 建立 TEST-ID，记录真实故障输入和预期结果，并在修复前失败、修复后通过后转为回归保护。
3. **Hermes 测试纪律**：登记标准测试入口及默认/专用分组；E2E 明确真实/模拟边界；Bug 保留真实故障形状；断言优先行为契约、不变量和正反结果；公共 fake/fixture 复用。

这次补充没有改变 §0 已拍板的 v1 技术边界：仍不新增 agent、command 或确定性执法脚本；扫描、判断和清单维护由主会话按 test-collaboration skill 在试点中执行。§16 的 `PASS` 针对补充前版本，不能自动覆盖本次新增范围。

请 Claude 后续只读复核以下六点并把结论追加为 §18：

1. 测试资产地图是否避免了“手抄全部测试函数”导致的腐烂；
2. “层级 + 用途”双标签是否正确区分单元/集成/E2E 与回归保护；
3. Bug 转 TEST-ID 是否既能机械要求证据，又为无法自动化的场景保留了明确人工验收出口。
4. “已覆盖必须进入标准测试入口或具名专用任务”是否与人工验收例外自洽；
5. E2E 的真实/模拟边界是否足以防止测试层级虚标；
6. 是否只吸收了 Hermes 可复用的测试纪律，而没有把大型仓库运行基础设施塞进 v1。

不要实现正式功能，不要运行双 Agent 自动脚本。输出 `CLAUDE VERDICT: PASS` 或附正文位置证据的 `CHANGES_NEEDED`。

## 18. Claude 只读复核（新增测试维护机制，2026-07-12）

CLAUDE VERDICT: CHANGES_NEEDED

小磊，已只读核对 §17 提出的三个问题（读了 §4.1-§4.4、§5 模板、§9"测试维护盘点"流程），未运行脚本、未改除本节外的任何内容。三问里两问通过，一问发现自相矛盾，按本文档一贯标准（此前"失效 vs 不适用"状态集矛盾被判定为阻塞项，见历史评审区第 1 轮）应先修再进入实现。

**阻塞问题（事实，已现场核对）**

1. **"层级 + 用途"双标签自相矛盾**（对应问题 2）。§4.2 明确定义"用途"只有两个受控取值：回归保护、安全/性能等专项。但紧接着的 §5 模板"现有测试资产地图"示例表"用途"列实际用了"规则保护"（订单金额行）和"关键链路"（完整下单行）——都不在 §4.2 的受控词表里。这份模板会被直接抄进 `templates/TESTS.example.md`，带着这个矛盾进实现，跟此前"失效"未进状态集是同一类问题，同等标准应先修：要么把"规则保护""关键链路"补进 §4.2 的受控词表，要么把 §5 示例改成只用 §4.2 已定义的两个值。

**非阻塞观察**

- （对应问题 1）测试资产地图靠"按模块/套件聚合"避免了"手抄全部函数"的规模问题，但正文没写清楚**什么事件触发重新盘点**——§9"测试维护盘点"只写了盘点五步，没写多久/何时该重跑一次。首次扫描后如果没人再触发，地图本身也会像"手抄清单"一样静默过期，且 v1 无脚本能发现（§0 拍板 #7）。不算阻塞，因为 v1 本来就是人工试点，但建议补一句显式触发条件（例如"试点首次扫描 + 每次向 TESTS.md 新增/关闭 TEST-ID 时顺带复核相关模块行"），否则 §13"识别至少一个缺失/重复/疑似废弃项"这条成功标准可能只在首次盘点时被满足一次，之后没人管。
- （对应问题 3）Bug 转 TEST-ID 机制本身自洽：§4.4 的证据清单是机械化的（来源/输入/预期/文件/节点/命令+修复前后状态），且 §9 Bug 修复流程第 6 步"没有 TEST-ID 或明确的人工验收理由，Bug 不得宣称闭环"把人工验收出口做成了强制二选一，不是可以被绕过的软要求。没有发现问题。

**结论**：修好 §4.2/§5 的用途取值矛盾即可复议；建议但不强制补盘点触发时机。其余部分未发现新的阻塞。

## 19. Codex 对 §18 的修订回执（2026-07-12）

`CODEX VERDICT: READY_FOR_CLAUDE_REVIEW`

§18 的一个阻塞项和一个非阻塞观察均已回写正文：

1. §4.2 现在将用途定义为四个受控值：`规则保护`、`关键链路`、`回归保护`、`专项保护`，允许多值并列；§5 模板全部使用该词表，不再出现未定义用途；
2. §4.1 增加测试资产地图的事件触发式维护规则：首次全量；测试配置/CI/TEST-ID/Bug/重大功能变化时增量复核；阶段交付前复核受影响范围；只有体系重构或普遍失真才全量重扫。

本次没有增加定时任务、Agent、Command 或执法脚本，v1 范围不变。请 Claude 只读确认上述两处是否消除 §18 的阻塞；若通过，追加 §20 `CLAUDE VERDICT: PASS`。不要实现正式功能，不要运行双 Agent 自动脚本。

## 20. Claude 只读确认（§18 修订核查，2026-07-12）

CLAUDE VERDICT: PASS

小磊，已只读核对 §19 声称的两处修订，未运行脚本、未改除本节外的任何内容。

**逐项核对**

1. **用途受控词表矛盾（§18 阻塞项）已消除**：§4.2 现定义四个受控值——规则保护、关键链路、回归保护、专项保护，并写明"用途必须从受控词表中选择，不能临时发明新值""受控用途值仅为……需要多个用途时用逗号并列"。§5 模板"现有测试资产地图"示例表四行（订单金额=规则保护、订单支付=回归保护、完整下单=关键链路、旧版皮肤=规则保护）与两条 TEST-ID 详情（TEST-ORDER-001=规则保护、TEST-BUG-035=回归保护）逐一核对，全部落在受控词表内，不再有游离值。

2. **盘点触发时机（§18 非阻塞观察）已补齐**：§4.1 新增"测试资产地图采用事件触发式维护，不设置容易失真的固定天数"，列出六条触发条件（首次全量、测试配置/CI 变化、TEST-ID/Bug 新增关闭、重大功能/契约/依赖变化、发布交付/`governance-sync`前、体系重构才全量重扫）。这是事件触发而非日历触发，和 module-regression 既有的"预警信号"式渐进采用哲学一致，解决了地图首次盘点后无人再触发的静默过期风险。

3. **v1 范围核查**：顺带核对 §11，"Hermes 借鉴纪律"被并入 §11 第 2 条"test-collaboration 方法论"这一已有交付物，没有新增 agent、command 或执法脚本，"第一版暂不实现"清单不变，与 §19 所述"v1 范围不变"一致。

**结论**：§18 提出的一个阻塞项和一个非阻塞观察均已用具体正文位置核实解决，未发现新的阻塞。PASS。

<!-- dual-agent-review:start -->
## 双 Agent 自动评审

> 此区域由 `scripts/dual-agent-review-loop.py` 管理；修改正文会触发新评审。
> 正文版本：`622aa1e51955`
> 评审时间：2026-07-12T09:12:42+08:00
> 最终状态：**达到最大轮数，仍需人工判断**

### 第 1 轮 · Claude

VERDICT: CHANGES_NEEDED

小磊，本轮已核对正文修订与事实源（module-regression SKILL、REGRESSION.example、全仓 grep「判定点」）。前两轮的主要阻塞已确实落回正文：§0 五项拍板齐全、§5 状态集四态自洽且删了`最近验证`、§6 写死了双 auditor 边界铁律、§11 砍到模板+skill。但迁移方案本身核对出新的阻塞问题。

**阻塞问题（事实，已逐一现场核对）**

1. **§3.1 迁移清单不完整，照单实现会留下悬空引用**。全仓 grep「判定点」显示引用共四处，§3.1 只覆盖两处：
   - `skills/module-regression/SKILL.md:64` 铁律4 要求"登记进该模块的判定点清单"——迁移后该清单已不在 REGRESSION.md，此句会指向不存在的位置；
   - `SKILL.md:42` 规定 `/regression-audit` 报"判定覆盖率"——数据源迁走后该指标由谁、从哪份文档算，未裁决；
   - `commands/governance-retro.md:19` 指向"module-regression 的判定点清单登记格式"——迁移后成死链。
   按方案自己的原则（迁移必须同一次改动完成、防两套教法），这三处不进 §3.1 清单，实现后插件内部立即漂移，且 governance-audit 自己就会查出这种死引用。

2. **§11 第 4 条"脚本执法"无落点**。文件/节点存在性、退出码检查"下沉到脚本"，但脚本住哪（插件 scripts/？目标项目？模板附带？）、v1 是否真做未写明——与拍板#2"砍到模板+skill"存在张力。建议二选一：写明交付路径与形式，或移到二期、v1 由人肉审查代行。

**建议（非阻塞）**

- §3.1 补齐后建议加一句验收：`grep -r 判定点` 全仓，除 TESTS.md 相关文件外零残留，作为迁移完成的机械判据。
- 判定覆盖率指标建议随判定点一起搬家：由 test-audit 侧（v1 人肉）统计，`/regression-audit` 不再报。

**推断**：上述两条补完即可 PASS，方向与范围无异议。

### 第 1 轮 · Codex

VERDICT: CHANGES_NEEDED

阿磊，审查结论：方案方向可行，但还有阻塞项，不能直接进入实现。

**事实 1：§3.1 迁移清单仍不完整。**
全仓现有功能文档里除 `skills/module-regression/SKILL.md` 的“判定点清单”正文和 `templates/REGRESSION.example.md` 外，仍有迁移后会悬空的引用：`skills/module-regression/SKILL.md` 铁律 4 还要求“登记进该模块的判定点清单”；`commands/governance-retro.md` 仍指向 `module-regression` 的判定点清单登记格式。只按 §3.1 实现，会违反本项目“方法论唯一源在 skill / 每个事实只活一处”的规则。

**事实 2：§11 v1 范围自相矛盾。**
§0 拍板 v1 是“模板 + skill”，但 §11 又把“文件存在性、测试节点存在性、命令退出码检查下沉到脚本执法”列入第一版。脚本放在插件 `scripts/`、目标项目模板、还是二期命令体系中，方案没有落点。

**建议修改：**
1. §3.1 补齐所有当前功能入口迁移项，至少包含 `module-regression` 铁律 4 与 `governance-retro` 的引用改写，并加验收：全仓 grep“判定点”，除历史评审/LOG 外无旧职责残留。
2. §11 明确二选一：v1 真包含脚本，则写清脚本文件名、归属、触发方式和失败退出码；否则把脚本执法移到二期，v1 仅模板 + skill + 人肉试点。

**推断：** 这两点补完后，TESTS.md 与 REGRESSION.md 的职责边界基本可以进入实现。

### 第 2 轮 · Claude

VERDICT: CHANGES_NEEDED

小磊，本轮复核结论：正文自第 1 轮以来逐字未变，我与 Codex 上一轮指出的两处阻塞仍未回落正文，维持不通过。

**事实（已重新现场核对）**

1. **§3.1 迁移清单仍不完整**。全仓 grep「判定点」确认，除 §3.1 已覆盖的两处（SKILL.md 判定点清单一节、REGRESSION.example.md 示例）外，仍有三处引用会因迁移变死链，且 §3.1 一条都没提及：
   - `skills/module-regression/SKILL.md:42`——"`/regression-audit` 顺带报缺口数（判定覆盖率 = 有执法命令的判定点 / 全部判定点）"，数据源迁走后这句话失去落点；
   - `skills/module-regression/SKILL.md:64`——铁律4"登记进该模块的判定点清单"，迁移后此清单已不在 REGRESSION.md；
   - `commands/governance-retro.md:19`——"指向 `module-regression` 的判定点清单登记格式"，迁移后指向空处。
   （`PROJECT_LOG.md:15` 的一条是历史流水账，不需要跟着改，不算漏项。）

2. **§11 第 4 条脚本执法仍无落点**。原文"下沉到脚本执法，不靠 agent 人肉"未指明脚本文件名、归属目录（插件 `scripts/`？目标项目？模板附带？）、v1 是否真交付，与 §0 拍板#2"v1 砍到模板+skill"仍有张力，未澄清是二选一还是并存。

**推断**：按现稿实现，铁律4和判定覆盖率两处会继续暗中依赖已被迁走的判定点清单，插件自己跑一次 `governance-audit` 就能查出这种死引用——即方案自己要治的病，会在自己身上复现。

**建议修改（非阻塞判断）**

- §3.1 补第 4、5 条：同步改写 SKILL.md:42 判定覆盖率描述的数据来源（挪去 TESTS.md 或改由 test-collaboration 侧统计）、SKILL.md:64 铁律4措辞（"判定点清单"→"TESTS.md 对应 TEST-ID"）、governance-retro.md:19 的指路目标；验收加一句机械判据：迁移后全仓 grep「判定点」，除历史 LOG/评审记录外零残留。
- §11 二选一并写清楚：要么明确脚本交付路径与 v1 范围，要么把脚本执法移到二期、v1 由人肉审查代行。

**推断**：以上两处补齐后我倾向 PASS，方案方向和整体切分本身无异议。

### 第 2 轮 · Codex

VERDICT: CHANGES_NEEDED

阿磊，事实：我按 §12 只读核对了正文和现有规则；状态集、`最近验证`、双 auditor 边界已回落正文，但仍有阻塞。

1. 事实：§3.1 迁移清单仍漏功能入口。现仓库 `skills/module-regression/SKILL.md:64` 仍要求“登记进该模块的判定点清单”；`commands/governance-retro.md:19` 仍指向 `module-regression` 的判定点清单登记格式。
推断：按现稿实现会留下死指路牌，违反“每个事实只活一处”。

2. 事实：§0 写 v1 砍到“模板 + skill”，但 §11 第 4 项又把文件存在性、测试节点存在性、命令退出码检查列入“脚本执法”，未定义脚本路径、触发方式和失败规则。
推断：v1 范围仍不自洽。

建议：§3.1 补齐上述两处引用迁移，并加 `grep 判定点` 验收；§11 明确脚本执法要么写清交付落点，要么移到二期，v1 只做人肉试点。

<!-- dual-agent-review:end -->
