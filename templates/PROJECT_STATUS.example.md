# PROJECT_STATUS.md —— 健康仪表盘（当前状态一眼看清；只写"现在怎样"，不写结构和历史）

> 本文件汇总有来源的健康快照；验证结论引用实际运行记录。相关实现或验收条件变化后标待复验，不能延续旧绿色结论。删除区里的文件已废弃，AI 和人都别再引用或重建它们。
> 顶部"红线块"每次进会话必读（危险不会自己报到）；下面的指标按需读。

## 🔴 红线块（每次进会话必读 —— 删除区 + 未决 P0/违规）

> 放这里的都是"不读就会悄悄踩雷"的东西：会被误重建的已删文件、不处理会出事的 P0。保持精简，几行为宜。

### 删除区（故意删掉，别再重建）
| 路径 | 为什么删 | 日期 | 替代物 |
|---|---|---|---|
| `src/legacy_parser.py` | 旧 v1 解析器，已被 services/parser 取代 | 2026-06-10 | `src/services/parser.py` |
| `scripts/old_sync.sh` | 同步逻辑搬进 Python | 2026-06-12 | `src/services/sync.py` |

### 未决违规 / P0 行动
- （填写已确认的紧急严重风险、触发条件、影响和证据；没有则写“暂无已确认 P0”。分级按 living-docs-governance Skill，勿将示例视为实际事故。）

## 验证缺口与维护线索（按需读）

- 示例：覆盖率 63%，需核对缺少保护的业务路径；风险级别由受影响路径决定。
- 示例：`services/report.py` 880 行，需检查职责和修改成本；行数本身不构成 P0。

### 审计保鲜度
上次审计：（日期）｜ 之后相关变更：（范围）｜ 复审触发条件：（按项目风险填写；间隔仅作提醒）

## 指标（按需读 —— 要看健康度时才读）

> 这部分按实际影响更新；指标越线时先核对风险，不机械刷新全部文档。LOG 记录有意义的结果与理由，省略重复操作叙事。以下仅为填写示例，不是通用阈值。

| 指标 | 现在 | 阈值 | 状态 |
|---|---|---|---|
| 关键业务路径 | （已验证范围与缺口） | 项目已确认的成功标准 | （按实际证据填写） |
| 测试覆盖率 | 63%（示例） | （项目约定及理由） | 待核对未覆盖路径 |
| 最大单文件行数 | 880（示例：services/report.py） | （仅在有维护依据时设置） | 维护线索，未定风险级别 |

本次验证依据：（提交或可定位的工作区版本、环境、检查范围、结果与证据链接；不可用项写未验证）。不同范围或版本的结论分别标注来源。
